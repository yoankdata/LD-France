# ==========================================
# CONFIGURATION NEON (PostgreSQL Serverless)
# ==========================================
# URL de connexion PostgreSQL depuis Neon
# Trouvable dans : Neon Dashboard > Project > Connection Details > Pooled Connection
DATABASE_URL="postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]?sslmode=require"

# ==========================================
# CONFIGURATION AUTHENTIFICATION (NextAuth.js)
# ==========================================
# Secret pour signer les JWT (générer avec: openssl rand -base64 32)
NEXTAUTH_SECRET="[GENERATE-A-RANDOM-SECRET-HERE]"

# URL de l'application
NEXTAUTH_URL="http://localhost:3000"

# ==========================================
# CONFIGURATION STORAGE (Optionnel - pour les images)
# ==========================================
# Option 1 : Vercel Blob (si déployé sur Vercel)
# BLOB_READ_WRITE_TOKEN="[YOUR-VERCEL-BLOB-TOKEN]"

# Option 2 : Cloudflare R2 (alternative S3)
# CLOUDFLARE_ACCOUNT_ID="[YOUR-ACCOUNT-ID]"
# CLOUDFLARE_R2_ACCESS_KEY_ID="[YOUR-ACCESS-KEY]"
# CLOUDFLARE_R2_SECRET_ACCESS_KEY="[YOUR-SECRET-KEY]"
# CLOUDFLARE_R2_BUCKET_NAME="ldfrance-images"

# ==========================================
# NOTES D'INSTALLATION
# ==========================================
# 1. Créer un projet Neon sur https://neon.tech (gratuit)
# 2. Copier ce fichier : cp .env.example .env.local
# 3. Remplacer DATABASE_URL avec ta connexion Neon
# 4. Générer NEXTAUTH_SECRET : openssl rand -base64 32
# 5. Installer les dépendances : npm install
# 6. Générer le client Prisma : npx prisma generate
# 7. Pousser le schéma : npx prisma db push
# ==========================================
